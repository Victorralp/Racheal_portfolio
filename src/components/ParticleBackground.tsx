// src/components/ParticleBackground.tsx

// Temporary fix: returning null component to avoid tsparticles version conflicts
const ParticleBackground = () => {
  return null;
};

export default ParticleBackground;
